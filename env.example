# This file contains example environment variables for configuring AI-Crew avatars.
# Copy this file to .env and fill in the correct values for your setup.

# --- Database Configuration (for RAG and Conversation History) ---
# PostgreSQL connection URL for the RAG knowledge base and conversation history.
# Ensure PostgreSQL is running and pg_vector extension is enabled.
# Example: postgresql://user:password@host:port/database_name
DATABASE_URL="postgresql://your_rag_user:your_password@localhost:5432/rag_db"

# --- Avatar-Specific Credentials ---
# Each avatar requires its own set of credentials for the services it interacts with.
# Replace 'AVATAR_ID' with the actual ID of your avatar (e.g., JOHN_DOE, JOHN_SMITH).
# The AVATAR_ID is derived from the name of your avatar's YAML profile file (e.g., john_doe.yaml -> JOHN_DOE).

# --- Example: Credentials for an avatar named 'JOHN_DOE' ---

# Email Connector (IMAP/SMTP)
# Used for sending and receiving emails.
JOHN_DOE_EMAIL_ADDRESS="john.doe@example.com"
JOHN_DOE_EMAIL_PASSWORD="your_email_password" # Use an App Password for Gmail/Outlook
JOHN_DOE_IMAP_SERVER="imap.example.com" # e.g., imap.gmail.com, outlook.office365.com
JOHN_DOE_SMTP_SERVER="smtp.example.com" # e.g., smtp.gmail.com, smtp.office365.com

# GitHub Connector
# Used for interacting with GitHub (e.g., commenting on issues/PRs).
JOHN_DOE_GITHUB_USERNAME="john_doe_gh"
JOHN_DOE_GITHUB_TOKEN="your_github_personal_access_token" # Requires 'repo' scope

# Telegram Connector
# Used for sending and receiving Telegram messages via a bot.
# Get your bot token from BotFather on Telegram.
JOHN_DOE_TELEGRAM_TOKEN="your_telegram_bot_token"

# Discord Connector
# Used for sending and receiving Discord messages via a bot.
# Get your bot token from the Discord Developer Portal.
JOHN_DOE_DISCORD_TOKEN="your_discord_bot_token"

# --- Example: Credentials for another avatar (e.g., 'JOHN_SMITH') ---
# JOHN_SMITH_EMAIL_ADDRESS="jane.doe@example.com"
# JOHN_SMITH_EMAIL_PASSWORD="your_email_password"
# JOHN_SMITH_IMAP_SERVER="imap.example.com"
# JOHN_SMITH_SMTP_SERVER="smtp.example.com"

# JOHN_SMITH_TELEGRAM_TOKEN="your_jane_telegram_bot_token"
# JOHN_SMITH_DISCORD_TOKEN="your_jane_discord_bot_token"
